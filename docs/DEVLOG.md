# BookCabinet — Журнал разработки (DEVLOG)

> Этот файл содержит историю решённых проблем. Перед тем как "изобретать велосипед" — проверь, не решали ли мы это раньше.

---

## 📅 Январь 2026

### [2026-01-10] RFID протокол — найдена причина молчания IQRFID-5102

**Проблема:** Считыватель IQRFID-5102 не отвечает на команды

**Причина:** Использовали неправильный протокол!

| ❌ Как делали | ✅ Как надо |
|--------------|-------------|
| Заголовок `0x04` | Заголовок `0xA0` |
| CRC-16 | Checksum `(~SUM + 1) & 0xFF` |
| Inventory cmd `0x01` | Inventory cmd `0x80` |

**Решение:** Создан скрипт `uhf_rfid_test.py` с правильным протоколом. Тестировать:
```bash
python3 uhf_rfid_test.py /dev/ttyUSB0
python3 uhf_rfid_test.py /dev/ttyUSB0 find  # поиск скорости
```

**Важно:** И карты и книги работают на UHF (860-960 MHz), поэтому IQRFID-5102 может заменить RRU9816.

---

### [2026-01-10] WiFi на RPi3 не работает

**Проблема:** `iwconfig` не показывает wlan0, NetworkManager видит сети но не подключается

**Диагностика:**
```bash
vcgencmd get_throttled  # Показало 0x50005 — under-voltage!
dmesg | grep -i wifi    # "mmc1: Failed to initialize"
```

**Причина:** Слабый блок питания — WiFi чип BCM43430 не стартует при под-напряжении

**Решение:** Заменить БП на 5V/2.5-3A (качественный, не китайский ноунейм)

---

### [2026-01-10] Тачскрин инвертирован по Y

**Проблема:** Касание вверху экрана регистрируется внизу

**Решение:** udev rule с матрицей трансформации:
```bash
sudo nano /etc/udev/rules.d/99-touchscreen.rules
```
```
ENV{ID_VENDOR_ID}=="222a", ENV{ID_MODEL_ID}=="0001", ENV{LIBINPUT_CALIBRATION_MATRIX}="1 0 0 0 -1 1 0 0 1"
```

---

### [2026-01-10] Датчики показывают инверсную логику

**Проблема:** Ожидали raw=1 когда нажат, получали raw=0

**Причина:** Датчики с NPN выходом — при срабатывании притягивают к земле

**Решение:** Инвертировать логику в коде:
```python
pressed = (GPIO.input(pin) == GPIO.LOW)  # НЕ HIGH!
```

---

## 📅 Декабрь 2025

### [2025-12-23] Python сервер не запускается на Windows

**Проблема:** BAT файл открывается и сразу закрывается

**Причина 1:** Путь содержит кириллицу — проблемы с кодировкой

**Причина 2:** Не установлен pyserial

**Решение:**
```cmd
pip install pyserial
cd C:\путь\к\проекту
python shelf_server.py COM8
```

---

## 📅 Сентябрь 2025

### [2025-09-14] Выбор технологии — почему Python + RPi

**Решение:** Используем Raspberry Pi напрямую для управления GPIO (без Arduino)

**Причины:**
- Один контроллер вместо двух (проще архитектура)
- Python для всего — сервер, GPIO, RFID
- Встроенные подтяжки GPIO — не нужны внешние резисторы
- Кроссплатформенная отладка (Windows + RPi)

---

## 🔧 Технические нюансы (помни всегда!)

### Терминология замков
```
Open  = язычок ОПУЩЕН (0°)  = полка свободна
Close = язычок ПОДНЯТ (95°) = захват полки
```
Это **противоположно** интуиции!

### Сетка ячеек
- 21 ряд: 0-20 (не 1-21!)
- 3 колонки: 0-2
- Окно выдачи: координаты (1, 9)

### RFID частоты
| Частота | Технология | Считыватель |
|---------|------------|-------------|
| 13.56 MHz | NFC/HF (MIFARE, ЕКП) | ACR1281U-C |
| 860-960 MHz | UHF (EPC Gen2) | IQRFID-5102, RRU9816 |

### GPIO датчиков на Raspberry Pi
Используем встроенную подтяжку RPi — внешние резисторы НЕ нужны:
```python
GPIO.setup(pin, GPIO.IN, pull_up_down=GPIO.PUD_UP)
```

### Конфигурация системы
- Все GPIO пины настраиваются в `config.py`
- После изменений: `sudo systemctl restart shelf-server`

---

## 🔗 Полезные ссылки

- Референс ИРБИС: https://github.com/valinerosgordov/RFIDShkafWithIRBIS
- Протокол UHF RFID: заголовок 0xA0, checksum = (~SUM + 1) & 0xFF
- ИРБИС API: TCP порт 6666, кодировка cp1251
