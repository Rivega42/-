# Устранение неполадок (TROUBLESHOOTING)

> Проблемы которые Claude НЕ решит сам — требуют физического вмешательства или внешних ресурсов.

---

## 🔴 Требуют физического доступа

### WiFi не работает, vcgencmd показывает 0x50005
```
Причина: Под-напряжение (слабый БП)
Решение: Заменить БП на 5V/2.5A+ (качественный!)
Проверка: vcgencmd get_throttled → должно быть 0x0
```

### RPi не загружается / красный LED мигает
```
Причина: Проблема с SD-картой или питанием
Решение: 
1. Проверить БП
2. Переписать образ на SD
3. Попробовать другую SD-карту
```

### Тачскрин не реагирует
```
Проверка: lsusb | grep -i touch
Если нет в списке → проверить USB кабель
Если есть → проверить udev rules
```

### RFID-ридер не определяется
```
Проверка: ls /dev/ttyUSB*
Если нет → проверить USB кабель, попробовать другой порт
Если есть → проблема в протоколе (см. DEVLOG)
```

### Мотор не крутится / сервопривод не двигается
```
Проверка:
1. Питание драйвера (обычно 12V/24V отдельно)
2. GPIO пины в config.py
3. sudo systemctl status shelf-server
```

---

## 🟡 Требуют внешних сервисов

### ИРБИС не отвечает
```
Проверка: telnet <irbis-server> 6666
Если не подключается → проблема на стороне библиотеки
Контакт: [добавить контакт IT библиотеки]
```

### Сеть недоступна
```
Проверка: ping 8.8.8.8
Если не пингуется → проверить роутер/свитч
WiFi SSID: [добавить]
```

---

## 🟢 Решаемые программно (но сложные)

### Калибровка координат ячеек
```
Файл: config.py или EEPROM
Нужно: физически замерить шаги до каждой ячейки
См.: CALIBRATION_MANUAL_v1_0.md
```

### Инверсия тачскрина
```
Файл: /etc/udev/rules.d/99-touchscreen.rules
Матрица: "1 0 0 0 -1 1 0 0 1" (инверсия Y)
После изменения: sudo udevadm control --reload-rules && sudo udevadm trigger
```

---

## 📞 Контакты

| Кто | Зачем | Контакт |
|-----|-------|---------|
| IT библиотеки | ИРБИС, сеть | [добавить] |
| Поставщик RFID | Замена оборудования | [добавить] |
| Поставщик механики | Запчасти | [добавить] |

---

*Добавляй проблемы которые встретились и потребовали "руками"*
